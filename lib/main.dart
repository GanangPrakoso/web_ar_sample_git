import 'package:flutter/material.dart';
import 'app_modules/web_ar/web_ar_app.dart';

void main() {
  runApp(MaterialApp(
      debugShowCheckedModeBanner: false,
      // theme: ThemeData(primarySwatch: Colors.indigo),
      home: WebArApp()));
}
